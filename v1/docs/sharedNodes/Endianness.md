# Endianness

The byte order of a numeric serialization.

## Variants

- `be` - Big-endian: the most significant byte is written first.
- `le` - Little-endian: the least significant byte is written first.
